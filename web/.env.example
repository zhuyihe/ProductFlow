# Leave empty in dev so Vite proxies /api to VITE_DEV_PROXY_TARGET from just web-dev.
# Leave empty in Docker Compose too: nginx serves the built app and proxies /api to atelier-backend.
# Set to an absolute backend URL only when serving the frontend separately.
VITE_API_BASE_URL=
